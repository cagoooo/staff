# 行政業務協調系統 - 進度報告 (2026-01-04)

## 📌 今日完成項目

### 1. 🏷️ 標籤即時更新修復
**檔案：** `js/tags.js`

**問題：** 在編輯行程模式下新增標籤後，前端沒有即時更新顯示，需要重新開啟才能看到新標籤。

**解決方案：**
- 在 `createAndAddTag` 函數中，將新標籤立即加入本地快取 `_globalTags`
- 強制重新渲染已選標籤顯示區域 `updateSelectedTagsDisplay()`
- 同時更新下拉選單列表以顯示新標籤

---

### 2. 🔧 確認對話框 z-index 修復
**檔案：** `index.html`

**問題：** 在事件視窗中點擊刪除按鈕時，確認對話框被事件視窗遮蓋住。

**解決方案：**
- 將 `sys-modal` 的 z-index 從 `z-[10000]` 提升到 `z-[99999]`
- 確保確認對話框永遠顯示在事件視窗之上

---

### 3. 🌅 全天行程功能
**檔案：** `index.html`, `js/firestore.js`, `js/event-modal.js`

**新功能：** 支援不指定時間的全天行程

**實作內容：**
1. **新增行程頁面 (`index.html`)**
   - 新增「🌅 全天行程（不指定時間）」勾選框
   - 時間欄位變成可選的（移除 required）
   - 新增 `toggleAllDay()` 函數，勾選後隱藏時間欄位

2. **資料儲存 (`js/firestore.js`)**
   - 新增 `isAllDay` 欄位
   - 全天行程的 `time` 欄位儲存為空字串

3. **編輯行程 (`js/event-modal.js`)**
   - 在編輯表單中新增全天勾選框
   - 新增 `toggleEditAllDay()` 函數
   - 載入時根據 `isAllDay` 設定勾選狀態和時間欄位顯示
   - 儲存時正確處理 `isAllDay` 和 `time` 欄位
   - 顯示時全天行程顯示「🌅 全天」而不是「--:--」

---

## 📝 Git Commits

| Commit Hash | 說明 |
|-------------|------|
| `ec43a5c` | fix: tags not updating immediately in edit mode after adding new tag |
| `2c63387` | fix: increase sys-modal z-index to ensure confirm dialog appears above event-modal |
| `f5b8532` | feat: add all-day event support - time field becomes optional |
| `924e846` | docs: update version to v3.6.0 with changelog for all-day events and bug fixes |

---

## 📊 版本資訊

- **版本號：** v3.5.0 → v3.6.0
- **更新日期：** 2026-01-04
- **CHANGELOG.md：** 已更新

---

## 🎯 明日待辦事項

> 可根據需求繼續優化：
> - 全天行程在日曆視圖的顯示優化
> - LINE 通知中全天行程的時間顯示處理
> - 其他使用者反饋的問題修復

---

明天見！💪
